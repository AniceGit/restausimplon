services:
  compose_postgres:
    image: postgres:16.9-alpine   # image officielle
    container_name: compose_postgres_container
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    networks:
      - mon_network
    volumes:
      - mon_volume:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 3s
      retries: 5

  compose_api_fastapi:    
    image: slehucher/restausimplon_api_image:1.1.1
    container_name: compose_api_fastapi_container
    restart: always
    depends_on:
      - compose_postgres
    ports:
      - "8000:8000"
    networks:
      - mon_network

  faker:
    image: slehucher/restausimplon_api_image:1.1.1
    container_name: restau_faker
    command: python -m app.scripts.fake_data
    depends_on:
      compose_postgres:
        condition: service_healthy
    networks:
      - mon_network

networks:
  mon_network:
    driver: bridge

volumes:
  mon_volume:

